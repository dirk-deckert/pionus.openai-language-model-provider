# Security

Please report security issues privately to `info@pionussolutions.com`. Do not
open a public issue for vulnerabilities or include credentials in diagnostics.

The extension reads credentials from VS Code secret storage or the user's
Codex authentication file. It must never log or commit access tokens.
